package bgu.spl.net.impl.stomp;

public class StompServer {

    public static void main(String[] args) {
        // TODO: implement this

        // if (args[1] == "tpc") {
        // ThreadPerClient<String> server = ThreadPerClient.<String>threadPerClient(
        // 7777,
        // () -> new StompMessagingProtocolImpl(),
        // () -> new StompEncoderDecoder());
        // server.serve();
        // }

        // if (args[1] == "reactor") {
        // Reactor<String> server = Reactor.<String>reactor(
        // Runtime.getRuntime().availableProcessors(),
        // 7777,
        // () -> new StompMessagingProtocolImpl(),
        // () -> new StompEncoderDecoder());
        // server.serve();
    }
}
